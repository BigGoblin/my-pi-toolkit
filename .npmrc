# Termux/Android has no @ast-grep/cli native binary; its postinstall exits 1 and
# aborts the whole `npm install`, leaving marked / @open-cursor/* missing.
# Runtime deps here are prebuilt or pure JS; install scripts are not required.
ignore-scripts=true
